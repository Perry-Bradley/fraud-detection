# Example only - DO NOT commit real secrets.
# Apply with: kubectl apply -f secrets.example.yml
# Or generate with kubectl create secret + Sealed Secrets / External Secrets in real life.
apiVersion: v1
kind: Secret
metadata:
  name: sms-secrets
  namespace: sms
type: Opaque
stringData:
  DB_NAME: school_fees
  DB_USER: sms_user
  DB_PASSWORD: change-me
  DJANGO_SECRET_KEY: change-me
  JWT_SECRET: change-me
