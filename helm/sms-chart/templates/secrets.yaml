# Demo only. In production, use SealedSecrets / ExternalSecrets / Vault.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
type: Opaque
stringData:
  DB_NAME: school_fees
  DB_USER: sms_user
  DB_PASSWORD: change-me
  DJANGO_SECRET_KEY: change-me
  JWT_SECRET: change-me
