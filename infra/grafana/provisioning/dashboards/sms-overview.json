{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "type": "stat",
      "title": "Backend HTTP Requests / sec",
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 0 },
      "targets": [
        { "expr": "sum(rate(django_http_requests_total_by_method_total[1m]))", "refId": "A" }
      ]
    },
    {
      "type": "stat",
      "title": "ML Service Anomaly Calls / sec",
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 0 },
      "targets": [
        { "expr": "sum(rate(http_requests_total{handler=\"/detect-anomaly\"}[1m]))", "refId": "A" }
      ]
    },
    {
      "type": "timeseries",
      "title": "Backend Latency (p95)",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 6 },
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(django_http_requests_latency_seconds_by_view_method_bucket[5m])) by (le))", "refId": "A" }
      ]
    }
  ],
  "schemaVersion": 39,
  "title": "SMS Overview",
  "uid": "sms-overview",
  "version": 1
}
